/* Hoisting (used for debugging) */
window.variables = () => variables();
window.passage = () => passage();
window.temporary = () => temporary();
window.mc = () => variables().mc;
window.npc = () => variables().npc;
window.clone = clone;

// window.memorize = () => memorize();
// window.recall = () => recall();
// window.forget = () => forget();

// window.Template = () => Template;
